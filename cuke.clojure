(def *stepdefs* (list "yo"))

(defn then [regexp, code]
  (cond stepdefs {:regexp regexp, :code code})

(then "I should have (.*) cukes in my belly"
  (fn [cukes] 
    (println "Hello," cukes)))
    
(println (first stepdefs))
