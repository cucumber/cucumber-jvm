Feature: Spring configuration using Annotated class.

  Scenario: Both injected beans shouldn't be null
    Given a new step
    When something happens
    Then injected beans shouldn't be nulls
    
  Scenario: Both injected beans shouldn't be null
    Given a new step
    When something happens
    Then injected beans shouldn't be nulls