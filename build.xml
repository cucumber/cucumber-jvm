<project name="cucumber-jvm" default="publish-artifacts">
    <property name="modules" value="core:junit:java:clojure:groovy:guice:ioke:jruby:jython:openejb:picocontainer:rhino:scala:spring:weld"/>

    <target name="publish-artifacts">
        <subant target="publish-artifacts" buildpath="${modules}"/>
    </target>

    <target name="generate">
        <subant target="generate" buildpath="${modules}"/>
    </target>

    <target name="clean">
        <subant target="clean" buildpath="${modules}"/>
    </target>

    <target name="git-clean">
        <exec executable="git">
            <arg value="clean"/>
            <arg value="-dfx"/>
        </exec>
    </target>
</project>
