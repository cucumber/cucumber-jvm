package cuke4duke
trait Italian { self: ScalaDsl =>
  val Dato = self.Given
  val Quando = self.When
  val Allora = self.Then
}
trait Welsh { self: ScalaDsl =>
  val AnrhegedigA = self.Given
  val Pryd = self.When
  val Yna = self.Then
}
trait Catalan { self: ScalaDsl =>
  val Donat = self.Given
  val Quan = self.When
  val Aleshores = self.Then
}
trait Norwegian { self: ScalaDsl =>
  val Gitt = self.Given
  val Når = self.When
  val Så = self.Then
}
trait Japanese { self: ScalaDsl =>
  val 前提 = self.Given
  val もし = self.When
  val ならば = self.Then
}
trait French { self: ScalaDsl =>
  val Soit = self.Given
  val Lorsque = self.When
  val Alors = self.Then
}
trait Texan { self: ScalaDsl =>
  val GivenYall = self.Given
  val WhenYall = self.When
  val ThenYall = self.Then
}
trait German { self: ScalaDsl =>
  val GegebenSei = self.Given
  val Wenn = self.When
  val Dann = self.Then
}
trait Czech { self: ScalaDsl =>
  val Pokud = self.Given
  val Když = self.When
  val Pak = self.Then
}
trait ChineseTraditional { self: ScalaDsl =>
  val 假設 = self.Given
  val 當 = self.When
  val 那麼 = self.Then
}
trait Slovak { self: ScalaDsl =>
  val Pokiaľ = self.Given
  val Keď = self.When
  val Tak = self.Then
}
trait Hungarian { self: ScalaDsl =>
  val Ha = self.Given
  val Majd = self.When
  val Akkor = self.Then
}
trait Russian { self: ScalaDsl =>
  val Допустим = self.Given
  val Если = self.When
  val То = self.Then
}
trait Finnish { self: ScalaDsl =>
  val Oletetaan = self.Given
  val Kun = self.When
  val Niin = self.Then
}
trait Spanish { self: ScalaDsl =>
  val Dado = self.Given
  val Cuando = self.When
  val Entonces = self.Then
}
trait Portuguese { self: ScalaDsl =>
  val Dado = self.Given
  val Quando = self.When
  val Então = self.Then
}
trait Korean { self: ScalaDsl =>
  val 조건 = self.Given
  val 만일 = self.When
  val 그러면 = self.Then
}
trait Estonian { self: ScalaDsl =>
  val Eeldades = self.Given
  val Kui = self.When
  val Siis = self.Then
}
trait Indonesian { self: ScalaDsl =>
  val Dengan = self.Given
  val Ketika = self.When
  val Maka = self.Then
}
trait Lolcat { self: ScalaDsl =>
  val ICanHaz = self.Given
  val Wen = self.When
  val Den = self.Then
}
trait Bulgarian { self: ScalaDsl =>
  val Дадено = self.Given
  val Когато = self.When
  val То = self.Then
}
trait Polish { self: ScalaDsl =>
  val Zakładając = self.Given
  val Jeżeli = self.When
  val Wtedy = self.Then
}
trait Australian { self: ScalaDsl =>
  val YaKnowHow = self.Given
  val YaGotta = self.Then
}
trait Arabic { self: ScalaDsl =>
  val بفرض = self.Given
  val متى = self.When
  val اذاً = self.Then
}
trait Swedish { self: ScalaDsl =>
  val Givet = self.Given
  val När = self.When
  val Så = self.Then
}
trait Romanian { self: ScalaDsl =>
  val Daca = self.Given
  val Cand = self.When
  val Atunci = self.Then
}
trait Hebrew { self: ScalaDsl =>
  val בהינתן = self.Given
  val כאשר = self.When
  val אז = self.Then
}
trait Danish { self: ScalaDsl =>
  val Givet = self.Given
  val Når = self.When
  val Så = self.Then
}
trait Vietnamese { self: ScalaDsl =>
  val Biết = self.Given
  val Khi = self.When
  val Thì = self.Then
}
trait Dutch { self: ScalaDsl =>
  val Gegeven = self.Given
  val Als = self.When
  val Dan = self.Then
}
trait Lithuanian { self: ScalaDsl =>
  val Duota = self.Given
  val Kai = self.When
  val Tada = self.Then
}
trait English { self: ScalaDsl =>
}
trait ChineseSimplified { self: ScalaDsl =>
  val 假如 = self.Given
  val 当 = self.When
  val 那么 = self.Then
}
trait RomanianDiacritical { self: ScalaDsl =>
  val Dacă = self.Given
  val Când = self.When
  val Atunci = self.Then
}
trait Latvian { self: ScalaDsl =>
  val Kad = self.Given
  val Ja = self.When
  val Tad = self.Then
}
trait Croatian { self: ScalaDsl =>
  val Zadan = self.Given
  val Kada = self.When
  val Onda = self.Then
}
